// import Cookies from 'js-cookie'


// const ACCESS_TOKEN = "ACCESS_TOKEN"
// const SLUG = "SLUG"
// const USER_SLUG = "USER_SLUG"


export class CookiesHandler {

    // static setSlugCookie(slugValue: string) {
    //     Cookies.set(SLUG, slugValue)
    // }

    // static setSlug(slug: string): void {
    //     Cookies.set(USER_SLUG, slug)
    // }

    // static setAccessToken(token: string | null): void {
    //     if (token) {
    //         Cookies.set(ACCESS_TOKEN, token)
    //     }
    // }
    // static getSlug(): string | undefined {
    //     return Cookies.get(USER_SLUG)
    // }

    // static getAccessToken(): string | undefined {
    //     return Cookies.get(ACCESS_TOKEN)
    // }

    // static removeSlug(): void {
    //     Cookies.remove(USER_SLUG)
    // }

    // static removeAccessToken(): void {
    //     Cookies.remove(ACCESS_TOKEN)
    // }
}